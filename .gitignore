# Generated scanners are CHECKED IN: they carry this fork's kcov "Cover:" line
# handling and _IOC decoding, which upstream syz-trace2syz does not have.
# Regenerate with `make generate` after editing the .rl/.y sources.
corpus.db
*.output
bin/
deserialized/
# Written by the -distill run; the path is set by the distillation config
# (getting-started/distill.json) and appended to on every run.
distillStats
